<?xml version="1.0" encoding="utf-8"?>
<!--
  Configuration for VoiceAccessService.

  canRequestFilterKeyEvents is the line that matters: without it Android never delivers
  volume-key presses to the service, and long-pressing volume down to talk would simply
  never fire.

  No event types are subscribed to beyond the minimum. The service watches one key and
  nothing on the screen, which is both a privacy choice and a battery one.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRequestFilterKeyEvents"
    android:canRequestFilterKeyEvents="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:summary="@string/accessibility_service_summary" />
